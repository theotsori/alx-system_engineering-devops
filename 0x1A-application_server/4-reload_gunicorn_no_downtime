#!/usr/bin/env bash
# Get the PID of the Gunicorn process
worker_pids=$(pgrep -f gunicorn)

kill -HUP "$worker_pids"

wait "$worker_pids"

gunicorn --bind 0.0.0.0:8001 --workers 4 web_flask.0-hello_route:app
